"""RBI-ObliBench / Agentic RAG compliance system.

Phase 0 establishes the shared foundation only:

* :mod:`src.common` — config, dual-mode paths, logging, I/O, retry, cache
* :mod:`src.schemas` — the record contracts all branches read and write

Everything else in this package is an interface stub awaiting its Phase 1 branch.
"""

__version__ = "0.1.0"
__phase__ = "phase0-base"
