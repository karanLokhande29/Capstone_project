# P1-004 (v3) — Corpus repair + three-rater pilot, run end-to-end from Claude Code

> **Replaces P1-004 v1 and v2. Do not run those.**
> Karan runs this in Claude Code. Claude Code performs every terminal step itself. Karan only places the three filled CSVs in `data/benchmark/tasks/` and clicks through Kaggle in a browser.

---

## ▶ FILLED IN (2026-09-25)

```
KARAN_MINUTES:        65   # basis: recalled_estimate (revised 2026-09-25)
KARAN_LABEL_SOURCE:   manual    # Karan confirmed on 2026-09-25 that each file was filled manually by its named rater
AKASH_MINUTES:        45   # basis: recalled_estimate (revised 2026-09-25)
AKASH_LABEL_SOURCE:   manual    # filled independently by Akash himself, without AI tools and without seeing other raters' files
MEER_MINUTES:         50   # basis: recalled_estimate (revised 2026-09-25)
MEER_LABEL_SOURCE:    manual    # filled independently by Meer himself, without AI tools and without seeing other raters' files
```

**Time rule.** Use the minutes above as given. Do not ask for them again.
- Every one is a `recalled_estimate`, not a timed measurement. Store `minutes_basis: recalled_estimate` for each in `annotation_log.json`, and show it next to every per-item time in every report.
- Any Phase 2 size projection built from these figures must be labelled "based on recalled estimates".
- The first **measured** figure comes from Part D, where Claude Code times Karan's pass 2 automatically. Once it exists, reports show it alongside the estimates.

**The three pilot CSV files.** Karan places all three directly in the project folder before running this prompt:

| File | Filled by | Minutes | What this prompt does with it |
|---|---|---|---|
| `data/benchmark/tasks/annotation_karan.csv` | Karan | ~65 (recalled estimate) | Validate, back up, ingest as Karan's pass 1. Never modify. |
| `data/benchmark/tasks/annotation_akash.csv` | Akash | ~45 (recalled estimate) | Validate, back up, ingest as rater `akash`. Never modify. |
| `data/benchmark/tasks/annotation_meer.csv` | Meer | ~50 (recalled estimate) | Validate, back up, ingest as rater `meer`. Never modify. |

**Provenance rule.** A file counts as a human rater's vote only if that named person filled it independently. If a file was filled by anyone other than its named rater, or with an AI tool, it must **not** be ingested under that rater's name. Record it as a diagnostic instead, exactly as A2's `ai_assisted` path does. Reporting it as that rater's vote would fabricate the inter-rater agreement.

**Blindness until Karan's pass 2.** Karan's blind pass 2 (Part D) is still ahead. Until Part D is finished:
- Claude Code prints **only aggregate** agreement numbers, never per-item votes, rationales or notes from any rater.
- Karan should not open `annotation_akash.csv`, `annotation_meer.csv` or `reports/phase1_pilot_disagreements.csv`.
- Adjudication of all disagreements happens **once**, in Part D, after pass 2.

**If a file is missing or blank at run time.** Don't block. Ingest what exists, print `awaiting <name>`, and carry on. Part C re-checks.

---

**A. Phase:** 1 — Corpus + Matrix (closure)
**B. Week:** Weeks 2–3 deliverables (annotation pilot, full corpus), closed late
**C. Prompt ID:** P1-004 (v3)
**D. Git:**
- Branch `wip/P1-004`, created from `main`.
- Allowed commands: `git status`, `git diff`, `git log`, `git add <in-scope files>`, `git commit`, `git push -u origin wip/P1-004` (Karan explicitly authorises pushing **this branch only**).
- Never push `main`, merge, tag, rebase, amend or force-push.
- Commit messages begin with `P1-004:`.

**E. Model To Use:** **Opus** (`claude --model opus`). This step includes a methodological gate (label provenance), agreement statistics, and re-measurement of the 60% trigger.

**K. Owner & Human Time:** Karan (sole developer).
- About 1 h supervising Part A.
- About 1.5–2 h in the browser for Part B (Kaggle).
- About 15 min for Part C.
- Akash and Meer: annotation already done (~45 and ~50 minutes, recalled estimates).

---

## F. Objective

1. **Pilot annotation.**
   - Validate all three filled files and record their provenance honestly.
   - Extend the tooling so **three human raters** (Karan, Akash, Meer) can label the same 18 items, giving pairwise Cohen's κ, Fleiss' κ and Karan's test-retest κ.
   - Keep any AI-assisted labels as a separate diagnostic, never as a rater.
   - Validate and ingest Akash's and Meer's filled files (already in the folder), and report agreement in aggregate only until Karan's pass 2.
2. **Corpus repair.**
   - Recover the 81 Directions that failed to download without re-downloading the 299 that succeeded, and fix the 3 empty titles.
   - Re-extract, re-segment, and re-run the matrix, QA and the cross-class alignment check.
   - Report per-entity-class coverage before and after.
   - All of it as a Kaggle notebook that runs from an uploaded Dataset, with outputs zipped for the return trip.
3. **Automate the return trip.** Unpack the Kaggle results, regenerate the reports, and assemble an audit bundle for Claude.

## G. Context (verified 2026-09-24/25 from committed data and Karan's file)

### Corpus
- **The failures.** 81/380 downloads failed with "received HTML where a PDF was expected". They are exactly the first 81 records in download order, which suggests a session-level block. That is an inference, and Task A8 tests it.
- **Missing Directions per entity class:**

  | Entity class | Missing / total |
  |---|---|
  | Commercial Banks | 35/44 |
  | Small Finance Banks | 31/40 |
  | Payments Banks | 4/27 |
  | Consumer Education and Protection | 7/7 |
  | Both Banker-to-Government headings | 2/2 each |

- **Code behaviour:**
  - `harvest_corpus()` has no cache check.
  - An HTML payload is never retried.
  - `extract_corpus()` iterates every record that has a `content_hash`. The 299 PDFs are not on Kaggle, so extraction must be filtered.
  - `segment_corpus()` re-segments every document that has a `.txt` and rewrites `paragraphs_index.jsonl`.
  - `document_id = md_<rbi_id>`, which is stable.
- **Paths on Kaggle.** `PathResolver` resolves to `/kaggle/working/data/...` and `/kaggle/working/reports/...`. The older notebooks copied inputs to `/kaggle/working/Capstone_project/data`, which is wrong.
- **Repository.** Public at `https://github.com/karanLokhande29/Capstone_project.git`.

### Karan's pass-1 file (`data/benchmark/tasks/annotation_karan.csv`)
- **Structurally valid.**
  - 18 rows. Columns and all 7 context columns are identical to the generated file.
  - Every `applies_to` name is canonical, every flag is valid, and there's no BOM.
  - 17 rows are voted: 7 `shared`, 7 `class-specific`, 3 `absent`.
  - 1 row is correctly left blank with a note ("Applicability/scope clause, not an obligation").
  - 3 rows have `applies_to` equal to the source class.
- **Methodological issues to handle:**
  1. **Provenance.** Five notes cite "per D8", a defect ID that exists only in the project's AI prompt documents. The rationales cite detailed 2026 regulatory facts across up to 12 classes, and the file came back quickly. Karan has declared the source `manual` (see the filled-in block). The master prompt forbids counting AI labels as a rater.
  2. **Unverifiable counterparts.** The rows citing Commercial Banks / Small Finance Banks counterparts for ICAAP, KYC and wilful defaulters rely on Directions among the 81 missing (e.g. md_13159, md_13127, md_13141, md_13109, md_13144, md_13112). They become checkable only after this repair.
  3. **Inconsistent function-heading rule.** "Payment and Settlement System" and "Foreign Exchange Management" were used in `applies_to` as stand-ins for the regulated party, while "Financial Market" and "Issuer of Currency" were mapped to bank classes instead. A written rule is needed before Akash and Meer label, or `applies_to` agreement is depressed by guideline ambiguity rather than by real disagreement.
  4. **Obligated-party ambiguity.** Row `t1_pilot_md_12927::p00046_00` ("they are required to submit an OVD…") places the duty on the customer, not the regulated entity. The instructions need an obligated-party rule.
- **The other two task files.** `annotation_akash.csv` (Akash) and `annotation_meer.csv` (Meer) have been filled and placed in `data/benchmark/tasks/` by Karan. P1-005's tooling currently ignores them because `second_raters: []`. A3 fixes that.
- **Current P1-005 tooling:**
  - Primary annotator `karan`; `second_raters: []`.
  - Stages: `ingest --pass`, `retest`, `adjudicate`, `report`.
  - Overwrite guards on task files.
  - Cohen's κ only vs primary pass 1. Fleiss' κ only when ≥3 raters.

## H. Why This Step Exists

- Phase 1 cannot close without a real reliability measurement on real annotations, and a corpus whose per-class coverage supports RQ1/RQ2.
- Both are currently blocked: one by provenance and guideline gaps, the other by the missing 81 Directions.

## I. Current Repository State

- `main` HEAD is the P1-005 commit, which this prompt verifies.
- Gitignored data present locally: `data/metadata`, `data/extracted` (299), `data/processed` (299 + index), `data/matrix`, `data/benchmark` (candidates, task files, Karan's filled file).
- The 299 PDFs are not local.

---

# PART A — Claude Code, local (runs now, no manual terminal work)

### A0 — Save this prompt, preflight, back up

1. Save this entire prompt, including Karan's filled-in block, to `docs/prompts/P1-004.md`. Parts C and D are resumed from this file.
2. `git status` must show no uncommitted changes to tracked files. `git log --oneline -5` must show a P1-005 commit. **Stop** if either fails.
3. `python3 -m pytest -q` must pass. Record the count.
4. Copy all of `data/benchmark/` to `~/oblibench_backups/P1-004_<timestamp>/`. Print the SHA-256 of every file.
5. Create branch `wip/P1-004`.

### A1 — Validate all three filled files (read-only)

Run for `annotation_karan.csv`, `annotation_akash.csv` and `annotation_meer.csv`. Check each against `pilot_candidates.jsonl` and `entity_classes.json`:
- columns;
- the 7 context columns are unchanged;
- no duplicate `label_id`;
- canonical class names only;
- valid flags;
- no flag without `applies_to`;
- the vote / blank / not_obligation status per row.

Print one table per file (ids, statuses, counts; no labels, rationales or notes) and write them to `reports/p1004_pilot_validation.md`. **Stop** on any structural error, naming the file and row.

**Independence check (data quality, aggregate only).** For each pair of files, compute the share of rows where `applies_to_rationale` or `notes` are identical, or near-identical (token Jaccard ≥ 0.9). Report only the counts per pair. If more than 3 rows in a pair are near-identical, print a **WARNING: possible non-independent annotation** and ask Karan to confirm the files were filled separately before ingesting that pair's second file.

### A2 — Record provenance and (conditionally) ingest pass 1

Read `KARAN_LABEL_SOURCE`. If it is still `__`, ask Karan.

**If `manual`:**
- Run `python3 scripts/run_annotation.py ingest --pass 1 --minutes karan=65`. Record `minutes_basis: recalled_estimate`.
- Print the earliest pass-2 date.
- Record `label_source: manual` in `data/benchmark/annotation_log.json`.

**If `ai_assisted`:**
- Copy the file to `data/benchmark/diagnostics/annotation_karan_ai_assisted.csv`.
- Record `label_source: ai_assisted` in the log.
- Restore a blank `annotation_karan.csv` via `build_annotation_tasks(force=True)`, which backs the old file up to `_overwritten/`.
- Do **not** ingest it as a vote.
- Print in plain words:
  - Karan has seen these labels, so his own pilot votes would not be independent.
  - For the pilot, Akash and Meer are the independent human raters.
  - The AI file is reported only as a "human–AI agreement" diagnostic.
  - Karan annotates by hand from Phase 2 onward.
- Karan's pilot test-retest is `NOT APPLICABLE (pilot labels AI-assisted)`.

### A3 — Three-rater tooling (`src/benchmark/annotation.py`, `scripts/run_annotation.py`, `config/config.yaml`)

Minimal changes. Keep all P1-005 behaviour and tests passing.

**Config:**
```yaml
benchmark:
  primary_annotator: karan
  second_raters: [akash, meer]
  ai_diagnostic_files: [diagnostics/annotation_karan_ai_assisted.csv]
```
The last key is present only if A2 took the `ai_assisted` branch.

**Rater provenance.** `annotation_log.json` records `label_source ∈ {manual, ai_assisted}` per (rater, pass). Only `manual` votes count as human votes.

**Agreement (`measure_agreement`).** Add the following. Every value comes with n, a bootstrap 95% CI (existing config seed), or `NOT YET MEASURED` with the reason.
- `pairwise`: Cohen's κ on `differential_flag`, raw agreement, `applies_to` exact-set match and mean Jaccard, for **every pair of human raters** on items both voted (karan-p1 / akash / meer).
- `fleiss_three_raters`: over items all three human raters voted. Only when three distinct human raters exist.
- `human_ai_diagnostic`: each human rater vs each AI diagnostic file, clearly labelled "not a reliability statistic".
- `not_obligation_agreement` across raters.
- Karan's `test_retest` stays as in P1-005.

**Promotion (pilot).**
- An item is `validated` when at least 2 human raters cast identical `(applies_to set, flag)` votes and no human rater's vote contradicts it without adjudication.
- `provenance` records `annotator:consensus:<ids>`.
- Otherwise the item is `in_review` and goes to `adjudicate`.
- The adjudication file lists every human vote plus the AI diagnostic, which is marked as such.

**`ingest --rater <id>`** ingests one second rater's file and prints that rater's counts. Validation is as in A1.

**Unit tests** (add to `tests/test_benchmark_solo_protocol.py` or a new `tests/test_benchmark_three_rater.py`):
1. Three raters never agree → pairwise and Fleiss κ < 0.2, no promotion.
2. Two of three agree → validated with consensus provenance.
3. An AI-assisted file never appears in `pairwise`/`fleiss` and never promotes an item.
4. A rater missing some rows → Fleiss uses only complete items and reports n.
5. `label_source=ai_assisted` for Karan → his votes are excluded from human statistics.

### A3b — Ingest Akash and Meer

- After A3's tests pass, run `python3 scripts/run_annotation.py ingest --rater akash --minutes akash=45`, then `python3 scripts/run_annotation.py ingest --rater meer --minutes meer=50`. Record `minutes_basis: recalled_estimate` for both.
- Record `label_source: manual` for each in `annotation_log.json`.
- Print **aggregate** agreement only: pairwise Cohen's κ, Fleiss' κ, raw agreement, `applies_to` exact-match and Jaccard, each with n and CI, plus minutes per item per rater. Never per-item content.
- Record that all three files were labelled under **guidelines v1**, because A4's v2 rules postdate them.

### A4 — Annotation guidelines v2 (`data/benchmark/templates/ANNOTATION_INSTRUCTIONS.md`)

Keep the existing definitions and add:
1. **Obligated party.** Label only obligations placed on a regulated entity. If the duty falls on a customer or third party (e.g. "they are required to submit an OVD"), leave `applies_to` and the flag blank and write `not an RE obligation — duty on <party>` in `notes`.
2. **Function headings.** Of the 19 names:
   - "Payment and Settlement System" may stand for authorised non-bank payment system operators.
   - "Foreign Exchange Management" may stand for authorised persons / AD entities.
   - "Issuer of Currency", "Financial Market", "Financial Inclusion and Development", "Consumer Education and Protection" and the two Banker-to-Government headings are **never** used in `applies_to`. Map to the regulated classes the text binds.
   - (This formalises the rule Karan's pass 1 mostly followed.)
3. **Counterpart citation.** For `shared` and `class-specific`, write `counterparts: md_xxxxx; md_yyyyy` in `notes`. For `absent`, write `checked: md_xxxxx; ...`. Use only Directions listed in `reference_directions.csv`, the corpus snapshot. Don't rely on Directions outside it.
4. **As-of rule.** Judge applicability as of the corpus snapshot. Note draft or future-effective provisions in `notes`, but don't let them change the label.
5. **Independence.** No AI tools (ChatGPT, Claude, Copilot, etc.) for labels, rationales or notes. Don't discuss items with the other annotators until all three files are submitted. Don't open anyone else's file.
6. **Time.** Record total minutes, then reply with the file and the minutes.

All three pilot files predate these rules. Record that they follow guidelines v1, and don't edit them. Guidelines v2 apply from Phase 2.

### A5 — Reference list for future annotation

Write `data/benchmark/templates/reference_directions.csv`. It is text-free: `document_id`, `entity_class_raw`, `title`, `source_url`, `in_corpus_now` (yes/no), one row per manifest entry. Annotators use it in Phase 2 to cite counterparts. No packets are needed: the pilot files are already filled.

### A6–A12 — Corpus repair (code)

**A6 — CLI (`scripts/run_harvest.py`).** Add these stages:
- **`diagnose [--from-failures] [--max 5]`.** Fetch each document twice, once with a fresh session and once after a listing warm-up. Log status, final URL, content type, length, the first 16 bytes and the HTML title. Save 4 KB of any HTML body to `logs/harvest_diagnose/` (gitignored).
- **`repair [--import-dir DIR] [--max-requests 150]`.** Run in order, stopping at the first failed check:
  1. diagnose
  2. only-missing harvest with retries
  3. manual PDF import
  4. extract only the new documents
  5. full re-segmentation
  6. stable-ID check and pilot-join check
  7. xref
  8. coverage report
- **`export-missing`.** Write `reports/p1004_missing_documents.csv` (id, url, url_basename, entity class, title; no RBI text).

**A7 — Scraper (`src/scraper/rbi_scraper.py`).** `harvest_corpus(only_missing, existing_manifest, max_requests)`:
- Records that already have a hash are carried through dict-equal.
- Download null-hash records plus newly listed ids (count them as `new_since_previous_harvest`).
- Keep ids that are no longer listed (count them as `no_longer_listed`).
- Stop before any request if the plan exceeds `max_requests`.
- Reuse a cache hit with no request.
- An HTML-instead-of-PDF payload is retried up to `network.payload_retries` (new key, default 3) with backoff, re-requesting the listing page before each retry.
- Record per document: attempts and outcome, in `payload_retry_outcomes`.
- Nothing beyond a normal session, warm-up and polite backoff.

**A8 — Manual PDF import.** `import_manual_pdfs(dir)`:
- Match by URL basename (case-insensitive) or by `<document_id>.pdf`.
- Check `%PDF` magic bytes, hash the file, and cache it under the same key downloads use.
- Update the manifest fields exactly as a download would.
- List the ids in `manual_import_ids`, noted "obtained by manual browser download from source_url".

**A9 — Empty titles** (`md_11959`, `md_12839`, `md_11510`).
- Inspect the listing markup. If the title exists elsewhere in the listing, fix the parser and re-derive `subject_family` for those 3 via the P1-002 path.
- Otherwise report it. Never take the title from the PDF.

**A10 — Extraction and consistency.**
- `extract_corpus(only_ids=...)` extracts only the new documents.
- Before re-segmenting, write the text-free `data/metadata/p1004_paragraph_fingerprints_before.json`, mapping `document_id → [[paragraph_id, sha256(text)]]`.
- Re-segment everything.
- **Stable-ID check.** For the original 299, the fingerprints must be identical. Any difference is a hard stop naming the document and the paragraph.
- **Pilot-join check.** Every pilot span must still resolve to the same paragraph and text.

**A11 — Coverage.** Add `coverage_by_entity_class` (before and after) to the harvest metrics and `reports/phase1_akash_corpus.md`, with a "Repaired by P1-004 (Karan, solo)" note. Any class below 90% prints a WARNING.

**A12 — Matrix, QA and alignment.**
- On Kaggle, run `run_matrix.py all` and `report`, then `run_annotation.py qa` and `checks` only.
- Add a fixed-sample option to `src/benchmark/alignment_check.py` and report two runs:
  - **(a)** as configured;
  - **(b)** Commercial Banks × Small Finance Banks × Payments Banks, crossed with the 3 subject families they share most often in the new Matrix v1, chosen mechanically.
- For each run give paragraph and section rates, baseline, n, and whether the trigger fired.
- "Before" figures come from the committed metrics JSONs.

**Unit tests (`tests/test_harvest_repair.py`, fixtures only):**
1. Only-missing requests only null-hash records and new ids; carried records stay dict-equal.
2. New and vanished ids are counted and kept.
3. The `max_requests` guard stops before any request.
4. HTML-then-PDF succeeds on attempt 2; always-HTML fails after the retry limit.
5. A cache hit makes no request.
6. Manual import matching, magic-byte check and cache key.
7. `only_ids` extraction touches only those ids.
8. The stable-ID check passes and fails correctly.
9. The pilot-join check fails when a paragraph is lost.
10. The coverage table and the <90% warning.
11. The fixed alignment sample is deterministic.
12. The notebook passes `nbformat.validate`, and every code cell parses with `ast.parse`.
13. Zip contents: the return zip excludes `data/benchmark` and `data/cache`; the v2 zip includes `pilot_candidates.jsonl` but no `annotation_*`.

Also run one fixture end-to-end `repair`.

### A13 — Kaggle notebook `notebooks/phase1-corpus-repair.ipynb`

Thin wrapper. Generate it with `nbformat` and validate it.

1. **Clone.** `REPO_URL` as above; `BRANCH = "wip/P1-004"` (clearly marked, to be changed to `main` after the merge). Print the commit.
2. **Dependencies.** `pip install -q pdfplumber`; print versions.
3. **Attach inputs.**
   - Walk `/kaggle/input` recursively to find the directory holding `metadata/document_manifest.jsonl`.
   - Copy `metadata`, `extracted`, `processed`, `matrix` and `benchmark/pilot_candidates.jsonl` into `/kaggle/working/data/`.
   - Detect any attached folder containing `.pdf`/`.PDF` files and treat it as `IMPORT_DIR`.
   - Assert 380 manifest rows and 299 processed documents.
4. **Tests.** Stop on failure.
5. **Diagnose.**
6. **Repair.** Pass `--import-dir` if `IMPORT_DIR` was found.
7. **Export missing.** If more than 0 remain, print the manual-download steps.
8. **Matrix.**
9. **QA + checks.** Print the (a)/(b) alignment table.
10. **Package.**
    - `/kaggle/working/p1004_return.zip`: `data/{metadata,extracted,processed,matrix}`, `reports/phase1_akash_*`, `reports/phase1_karan_*`, `reports/phase1_meer_{qa,checks}_metrics.json`, `reports/p1004_*`, `logs/harvest_diagnose/`. **Excludes** `data/benchmark`, `data/cache` and `phase1_meer_annotation.md`.
    - `/kaggle/working/rbi-oblibench-corpus-v2.zip`: the above data folders plus `data/cache` and `pilot_candidates.jsonl`.
    - Assert neither zip contains `annotation_`.
11. **Summary.** Print every metric in §Z.

### A14 — Test, commit, push, package for Kaggle

1. `python3 -m pytest -q`. Everything must pass.
2. Commit on `wip/P1-004`: code, tests, notebook, config, instructions, `reference_directions.csv`, `docs/prompts/P1-004.md` and `reports/p1004_pilot_validation.md`. No files under `data/` other than `templates/`.
3. `git push -u origin wip/P1-004`.
4. Build `~/Desktop/rbi-oblibench-corpus-v1.zip` from `data/metadata data/extracted data/processed data/matrix data/benchmark/pilot_candidates.jsonl`, excluding `*.DS_Store`. Assert it contains no `annotation_` file. Print its size.
5. Re-hash `data/benchmark/`. Every change must be explained: the ingests, the log and the votes files. The three filled CSVs must be byte-identical to the A0 backup.
6. Open `https://www.kaggle.com/datasets` and `~/Desktop` with `open`.
7. **Print the Part B checklist below, verbatim,** with the actual file paths filled in.

---

# PART B — Karan, in the browser only

**B1. Upload the corpus to Kaggle.**
- **Create → New Dataset** → upload `~/Desktop/rbi-oblibench-corpus-v1.zip`.
- Title `rbi-oblibench-corpus-v1`, **Private**, then Create.

**B2. Import and set up the notebook.**
- **Create → New Notebook → File → Import Notebook** → select `notebooks/phase1-corpus-repair.ipynb` from the repo folder.
- **Settings → Internet: On** (needs a phone-verified Kaggle account).
- **Add Input** → `rbi-oblibench-corpus-v1`.

**B3. Run the notebook.**
- **Run All** in the interactive session. Do **not** use Save & Run All.

**B4. Download the outputs.**
- From the `/kaggle/working` output panel, download `p1004_return.zip` and `rbi-oblibench-corpus-v2.zip` into `~/Downloads/`.
- Upload `rbi-oblibench-corpus-v2.zip` as a new private Dataset `rbi-oblibench-corpus-v2`.

**B5. Only if Cell 7 lists missing documents:**
- Download those PDFs from their `source_url` in a normal browser, keeping the original filenames.
- Upload them as a private Dataset `rbi-missing-pdfs`.
- **Add Input** it to the notebook and **Run All** again.

**B6. Resume in Claude Code** (in the repo): type `Run Part C of docs/prompts/P1-004.md`.

---

# PART C — Claude Code, resume after Kaggle (idempotent; re-run whenever a new file arrives)

**C1. Kaggle return.**
- If `~/Downloads/p1004_return.zip` exists, list its contents and **stop** if it contains `data/benchmark` or `annotation_`.
- Back up the current `data/{metadata,extracted,processed,matrix}` to `~/oblibench_backups/P1-004_pre_unzip_<timestamp>/`.
- Unzip over the repo root.

**C2. Rater files re-check.**
- Compare the hashes of the three filled CSVs with the A0 backup.
- If Karan has replaced a file in place (hash changed), ask him to confirm it's the named rater's final version, then re-validate (A1) and re-ingest that rater only.
- If a file is still blank, print `awaiting <name>`.

**C3. Report (aggregate only).**
- Run `python3 scripts/run_annotation.py report`.
- Print aggregate agreement only; the blindness rule still applies.
- Do **not** run `adjudicate` here. Adjudication happens once, in Part D.

**C4. Tests and commit.**
- `python3 -m pytest -q`.
- Commit the updated tracked reports on `wip/P1-004` and push the branch.

**C5. Audit bundle for Claude.**
- Write `reports/p1004_audit_bundle.md` containing:
  - pytest totals;
  - the Kaggle summary metrics from the unzipped metrics JSONs;
  - coverage before and after;
  - the stable-ID and pilot-join results;
  - matrix before and after;
  - alignment (a)/(b);
  - the pilot agreement block (pairwise, Fleiss, test-retest if applicable, human–AI diagnostic);
  - minutes per item per rater;
  - the provenance declaration;
  - the list of still-missing documents.
- Zip it, together with `reports/phase1_akash_corpus.md`, `phase1_karan_matrix.md`, `phase1_meer_annotation.md`, `p1004_pilot_validation.md` and `p1004_missing_documents.csv`, to `~/Desktop/P1-004_audit_bundle.zip`.
- Open the Desktop.
- Tell Karan: "Send `P1-004_audit_bundle.zip` to Claude for the Phase 1 audit."

---

# PART D — Claude Code, Karan's blind pass 2 (only if `KARAN_LABEL_SOURCE = manual`)

Trigger: Karan types `Run Part D of docs/prompts/P1-004.md` on or after the earliest pass-2 date.

1. Run `python3 scripts/run_annotation.py retest`. Do not use `--allow-short-gap` unless Karan explicitly asks, and record it if he does.
2. Record the current UTC time as the pass-2 start in `annotation_log.json`. Then open `annotation_karan_pass2.csv` with `open`, and do **not** display pass-1 content anywhere in the session. Tell Karan to fill it without opening his pass-1 file, then say "pass 2 done".
3. When Karan says "pass 2 done", compute the elapsed minutes from the recorded start. Show him the figure and let him subtract any breaks he tells you about. Then run `ingest --pass 2 --minutes karan=<elapsed>` with `minutes_basis: measured`, then `report`.
4. The blindness rule now ends. Run `python3 scripts/run_annotation.py adjudicate`. It covers **both** Karan's pass-1-vs-pass-2 disagreements **and** disagreements between Karan, Akash and Meer.
   - Open the adjudication CSV and tell Karan to write a rationale for every final decision, then say "adjudication done".
   - When he does, run `ingest --adjudication`, then `report`.
5. Regenerate the audit bundle (C5) and commit and push the reports.

---

## M. Files To Inspect

- `src/scraper/rbi_scraper.py`, `src/extraction/text_extractor.py`, `src/preprocessing/{segmenter,cross_references}.py`
- `scripts/run_{harvest,matrix,annotation}.py`
- `src/benchmark/{annotation,alignment_check,pilot}.py`, `src/metadata/vocabulary_discovery.py`
- `src/common/*` (read-only), `config/config.yaml`
- the existing notebooks and tests

## N. Files To Create

- `docs/prompts/P1-004.md`
- `notebooks/phase1-corpus-repair.ipynb`
- `tests/test_harvest_repair.py`, `tests/test_benchmark_three_rater.py`
- `data/benchmark/templates/reference_directions.csv`
- `reports/p1004_pilot_validation.md`

## O. Files To Modify

- `src/scraper/rbi_scraper.py`
- `src/extraction/text_extractor.py` (`only_ids` only)
- `src/benchmark/annotation.py` (A3 only)
- `src/benchmark/alignment_check.py` (fixed sample only)
- `scripts/run_harvest.py`, `scripts/run_annotation.py`
- `config/config.yaml` (`network.payload_retries`, `benchmark.second_raters`, `benchmark.ai_diagnostic_files`)
- `data/benchmark/templates/ANNOTATION_INSTRUCTIONS.md`
- `README.md` (a short "Kaggle workflow (P1-004)" section)

**Must not change:** `src/schemas/**`, `src/common/**`, the segmentation logic, the content of any filled annotation file, the older notebooks.

## P. Data Schema

No schema change. New metrics keys:
- `coverage_by_entity_class`, `payload_retry_outcomes`, `manual_import_ids`
- `new_since_previous_harvest`, `no_longer_listed`
- `stable_id_check`, `pilot_join_check`
- `alignment_fixed_sample`, `diagnosis`
- `pairwise`, `fleiss_three_raters`, `human_ai_diagnostic`

## R/S. Errors and Logging

- Per-document failures are logged and counted and never abort the run.
- Consistency checks and the request guard abort the run before downstream metrics, and name the cause.
- INFO per download attempt and per annotation file. WARNING per class below 90% and per excluded AI-assisted file.

## V. Evaluation Checkpoint

1. **What:** corpus coverage and consistency, plus pilot reliability.
2. **Why:** RQ1/RQ2 rest on per-class coverage and label reliability.
3. **Metrics:** per-class download rate, stable-ID/pilot-join checks, before/after matrix and alignment, pairwise and Fleiss κ with CI and n, minutes per item.
4. **Baseline:** the committed pre-repair metrics; κ = 0 (chance).
5. **Artifact:** `reports/p1004_audit_bundle.md`.
6. **PASS:** all classes ≥ 90%, or failures diagnosed; both checks pass; all three human files validated and ingested; Fleiss' κ measured.
7. **WARNING:** class gaps with reasons; a rater file missing or blank; the independence check flagged a pair.
8. **FAIL:** a consistency check fails; any AI label counted as a rater.
9. **Progression:** PASS or WARNING-with-reasons → Phase 1 Audit.

## W. Baseline

- 299/380 documents; Commercial Banks 9/44; Small Finance Banks 9/40.
- 51,853 paragraphs; matrix 295/1,064 cells.
- Alignment 36.5% (paragraph) / 34.2% (section).

## Z. Metrics To Collect

- **Diagnosis:** result, and whether the warm-up helped.
- **Documents:** discovered / previously downloaded / downloaded now / manually imported / still failed (with reasons).
- **Harvest bookkeeping:** new and vanished ids.
- **Coverage:** per class, before and after.
- **Extraction:** new-document success / empty / failed.
- **Paragraphs:** total, before and after.
- **Checks:** stable-ID and pilot-join results.
- **Matrix:** before and after.
- **Alignment:** (a)/(b).
- **Empty titles:** outcome for the 3 documents.
- **Pilot:** votes per rater; not-obligation count; pairwise κ; Fleiss κ; test-retest κ (or N/A); human–AI diagnostic; minutes per item per rater; validated and in-review counts.

## AB. Stop Conditions

Stop and report if:
- the preflight fails;
- a schema field is needed;
- the stable-ID check fails;
- the planned requests exceed 150;
- the host blocks all automation (finish the manual-import path; add no evasion);
- a provenance value is missing and Karan doesn't answer;
- a rater file fails validation, or the independence check fires and Karan does not confirm the files were filled separately.

## AC. Do Not Implement Yet

- FAQ/enforcement harvesting.
- Phase 2 extraction, matching or sampling.
- Any LLM labelling.
- Entity-class vocabulary redesign (D8). The A4 rule is an annotation guideline only.
- Temporal parsing beyond `update_date`.

## AD. Merge

No merge in this prompt. After Claude's Phase 1 Audit passes, Claude will issue the merge instruction. Karan pastes it into Claude Code, and it merges `wip/P1-004` into `main` fast-forward, pushes, and deletes the branch. No tag until then.

## AE. Logbook

- Documents downloaded: [actual] / [actual]
- Commercial Banks coverage: [actual]; Small Finance Banks coverage: [actual]
- Paragraphs: [actual]; matrix populated cells: [actual]
- Alignment (a) / (b): [actual] / [actual]
- Pilot raters: [actual]; Fleiss κ: [actual]; pairwise κ: [actual]; test-retest κ: [actual or N/A]; minutes/item: [actual]

## AF. Hand-Off

`~/Desktop/P1-004_audit_bundle.zip` goes to Claude for the Phase 1 Audit, together with P1-005's `pytest -q` output and Claude Code's summary if not already sent.
